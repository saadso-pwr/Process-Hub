# Copy to .env and adjust. The default matches docker-compose.yml.
DATABASE_URL="postgresql://process:process@localhost:5433/process_hub?schema=public"

# Optional — enables live "Converge with AI" (otherwise the built-in parser is used).
# ANTHROPIC_API_KEY=sk-ant-...
# CONVERGE_MODEL=claude-sonnet-4-6
